# 本地缓存架构图

## 1. 三级缓存架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           三级缓存架构总览                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                  │
│   │   L1缓存层   │     │   L2缓存层   │     │   L3存储层   │                  │
│   │  Caffeine   │     │    Redis     │     │   MySQL     │                  │
│   │  (本地缓存)  │     │ (分布式缓存)  │     │  (数据库)    │                  │
│   └──────┬──────┘     └──────┬──────┘     └──────┬──────┘                  │
│          │                   │                   │                         │
│    纳秒级访问            毫秒级访问            秒级访问                      │
│    (JVM堆内存)          (网络+内存)           (磁盘IO)                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 2. 查询流程时序图

```
┌─────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  用户请求  │    │  L1本地缓存   │    │  L2 Redis    │    │  L3 数据库    │
└────┬─────┘    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘
     │                 │                  │                  │
     │  查询商户详情     │                  │                  │
     │ ──────────────> │                  │                  │
     │                 │                  │                  │
     │                 │  getIfPresent()  │                  │
     │                 │ ───────────────> │                  │
     │                 │                  │                  │
     │                 │   命中(纳秒级)    │                  │
     │                 │ <─────────────── │                  │
     │                 │                  │                  │
     │   直接返回结果     │                  │                  │
     │ <─────────────── │                  │                  │
     │                 │                  │                  │
     │                 │   未命中          │                  │
     │                 │ ───────────────> │                  │
     │                 │                  │                  │
     │                 │                  │  get()查询Redis  │
     │                 │                  │ ───────────────> │
     │                 │                  │                  │
     │                 │                  │   命中(毫秒级)    │
     │                 │                  │ <─────────────── │
     │                 │                  │                  │
     │                 │   put()回填L1     │                  │
     │                 │ <─────────────── │                  │
     │                 │                  │                  │
     │   返回结果        │                  │                  │
     │ <─────────────── │                  │                  │
     │                 │                  │                  │
     │                 │                  │   未命中          │
     │                 │                  │ ───────────────> │
     │                 │                  │                  │
     │                 │                  │                  │  查询数据库
     │                 │                  │                  │ ──────────>
     │                 │                  │                  │
     │                 │                  │                  │   查到数据
     │                 │                  │                  │ <─────────
     │                 │                  │                  │
     │                 │                  │  set()回填L2     │
     │                 │                  │ <─────────────── │
     │                 │                  │                  │
     │                 │   put()回填L1     │                  │
     │                 │ <─────────────── │                  │
     │                 │                  │                  │
     │   返回结果        │                  │                  │
     │ <─────────────── │                  │                  │
     │                 │                  │                  │
```

## 3. 更新流程时序图

```
┌─────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  用户更新  │    │  当前节点    │    │  Redis      │    │  其他节点    │    │  数据库      │
└────┬─────┘    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘    └──────┬──────┘
     │                 │                  │                  │                  │
     │  更新商户信息     │                  │                  │                  │
     │ ──────────────> │                  │                  │                  │
     │                 │                  │                  │                  │
     │                 │  更新数据库       │                  │                  │
     │                 │ ───────────────> │                  │                  │
     │                 │                  │                  │                  │
     │                 │   更新成功        │                  │                  │
     │                 │ <─────────────── │                  │                  │
     │                 │                  │                  │                  │
     │                 │  删除L1缓存       │                  │                  │
     │                 │ ───────────────> │                  │                  │
     │                 │                  │                  │                  │
     │                 │  删除Redis缓存    │                  │                  │
     │                 │ ───────────────> │                  │                  │
     │                 │                  │                  │                  │
     │                 │  发布失效消息      │                  │                  │
     │                 │ ───────────────> │                  │                  │
     │                 │                  │                  │                  │
     │                 │                  │  广播失效消息      │                  │
     │                 │                  │ ───────────────> │                  │
     │                 │                  │                  │                  │
     │                 │                  │                  │  删除L1缓存      │
     │                 │                  │                  │ ──────────────> │
     │                 │                  │                  │                  │
     │                 │   返回成功        │                  │                  │
     │                 │ <─────────────── │                  │                  │
     │                 │                  │                  │                  │
     │   更新成功        │                  │                  │                  │
     │ <─────────────── │                  │                  │                  │
     │                 │                  │                  │                  │
```

## 4. 多节点缓存一致性机制

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     多节点缓存一致性解决方案                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────┐          ┌─────────────┐          ┌─────────────┐         │
│   │   节点A     │          │   Redis     │          │   节点B     │         │
│   │ (更新数据)  │          │  (Pub/Sub)  │          │ (消费节点)  │         │
│   └─────┬───────┘          └──────┬──────┘          └──────┬──────┘         │
│         │                         │                       │                │
│         │  1. 更新数据库           │                       │                │
│         │ ──────────────────────> │                       │                │
│         │                         │                       │                │
│         │  2. 删除本地L1缓存       │                       │                │
│         │ ──────────────────────> │                       │                │
│         │                         │                       │                │
│         │  3. 删除Redis L2缓存     │                       │                │
│         │ ──────────────────────> │                       │                │
│         │                         │                       │                │
│         │  4. 发布失效消息          │                       │                │
│         │ ──────────────────────> │                       │                │
│         │                         │                       │                │
│         │                         │  5. 广播消息给订阅者    │                │
│         │                         │ ────────────────────> │                │
│         │                         │                       │                │
│         │                         │                       │  6. 删除本地L1缓存 │
│         │                         │                       │ ──────────────> │
│         │                         │                       │                │
│         │  7. 返回更新成功          │                       │                │
│         │ <────────────────────── │                       │                │
│         │                         │                       │                │
└─────────┴─────────────────────────┴───────────────────────┴────────────────┘
```

## 5. 缓存命中率与性能对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       缓存命中率与性能对比                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  缓存层级    │  命中率  │  访问速度  │  存储位置  │  一致性  │  容量限制        │
│  ───────────┼─────────┼───────────┼───────────┼─────────┼───────────────── │
│  L1本地缓存  │  80-90%  │  纳秒级    │  JVM堆内存  │ 最终一致  │  受JVM堆限制     │
│  (Caffeine) │          │ (1-100ns) │           │         │  (1000条商户)    │
│  ───────────┼─────────┼───────────┼───────────┼─────────┼───────────────── │
│  L2 Redis   │  10-15%  │  毫秒级    │ Redis服务端 │ 强一致    │  受Redis内存限制  │
│  (分布式)    │          │ (1-10ms)  │           │         │  (无限制)        │
│  ───────────┼─────────┼───────────┼───────────┼─────────┼───────────────── │
│  L3 数据库   │  5%以下   │  秒级      │  磁盘      │ 强一致    │  受磁盘容量限制   │
│  (MySQL)    │          │ (10ms-1s) │           │         │  (TB级别)        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 6. 核心代码调用关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         核心代码调用关系图                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ShopController.queryById()                                               │
│         │                                                                   │
│         ▼                                                                   │
│   ShopServiceImpl.queryById()                                               │
│         │                                                                   │
│         ▼                                                                   │
│   CacheClient.queryWithLocalCache()                                         │
│         ├─ 1. localCache.getIfPresent(id)  // 查L1本地缓存                   │
│         ├─ 2. queryWithPassThrough()       // 查L2→L3穿透查询                │
│         └─ 3. localCache.put(id, result)   // 回填L1缓存                    │
│                                                                             │
│   ShopController.update()                                                   │
│         │                                                                   │
│         ▼                                                                   │
│   ShopServiceImpl.update()                                                  │
│         ├─ 1. updateById(shop)             // 更新数据库                    │
│         ├─ 2. cacheClient.delete(key)      // 删除Redis缓存                 │
│         ├─ 3. cacheClient.deleteShopLocalCache(id) // 删除本地缓存           │
│         └─ 4. cacheInvalidationPublisher.publishShopInvalidation(id)       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 7. 缓存配置参数

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           缓存配置参数对比                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  缓存类型        │  商户缓存 (shopLocalCache)  │  商户类型缓存 (shopTypeLocalCache) │
│  ───────────────┼───────────────────────────┼────────────────────────────── │
│  Key类型         │  Long (商户ID)             │  String ("shop-type:list")      │
│  Value类型       │  Shop对象                  │  List<ShopType>列表             │
│  最大条目数       │  1000条                   │  100条                          │
│  写入后过期时间    │  5分钟                    │  10分钟                         │
│  淘汰策略        │  W-TinyLFU算法             │  W-TinyLFU算法                  │
│  使用场景        │  商户详情页                │  首页商户类型列表                │
│  一致性保障      │  Pub/Sub广播+TTL兜底       │  Pub/Sub广播+TTL兜底             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

这个架构图清晰地展示了项目的三级缓存设计，从用户请求到数据返回的完整流程，以及多节点环境下的缓存一致性解决方案。